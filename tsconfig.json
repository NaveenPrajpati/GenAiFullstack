{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // No `baseUrl`: it is deprecated in TypeScript 6 and unnecessary, since
    // `paths` has resolved relative to this file since TS 5.0.
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["jest"]
  }
}
